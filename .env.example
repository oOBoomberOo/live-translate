# Google Cloud API key with Cloud Translation API + Cloud Vision API enabled.
# Used by the visual test suite (npm run test:visual) and can also be pasted
# into the extension Settings UI for manual use.
GOOGLE_API_KEY=
