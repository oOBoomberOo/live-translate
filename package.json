{
  "name": "live-translate-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "icons": "node scripts/generate-icons.mjs",
    "test:e2e": "npm run build && node e2e/generate-fixtures.mjs && node e2e/run-extension-test.mjs",
    "test:visual": "npm run build && node e2e/visual-site-test.mjs"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@types/chrome": "^0.0.287",
    "playwright": "^1.61.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  }
}
